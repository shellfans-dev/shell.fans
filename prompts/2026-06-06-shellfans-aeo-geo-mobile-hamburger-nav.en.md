# Claude Code Prompt (English) — Fix /aeo-geo mobile nav to use the same hamburger as shell.fans homepage

Task: make `https://shell.fans/aeo-geo` mobile nav behave like the
`https://shell.fans` homepage mobile nav (hamburger button, tap to open
drawer/menu).

Requirements:
- show hamburger icon on small screens
- tap to expand the mobile menu (same or highly consistent with homepage)
- do not break desktop nav
- reuse the homepage's header / drawer / breakpoint logic where it exists
- self-check, self-validate, commit + push to GitHub when done
- full original prompt is in the conversation context
